name: Build_imm_高通410系列

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: '← 选择是否开启 SSH 调试，专业人员使用'
        required: false
        default: false
        type: boolean
      profile:
        description: '↓ 支持多款型号，手动选择要编译的机型 ↓'
        type: choice
        options:
          - ufi003
          - ufi001c
          - ufi001b
          - ufi103s
          - jz02v10
          - qrzl903
          - w001
          - uz801
          - mf32
          - mf601
          - wf2
          - sp970v11
          - sp970v10
        default: ufi003
        required: true
      upstream:
        description: '↓ 选择上游源码仓库 ↓'
        type: choice
        options:
          - lkiuyu/immortalwrt
          - xuxin1955/immortalwrt
        default: lkiuyu/immortalwrt
        required: true
      extra_packages:
        description: '↓ 输入额外包名，逗号或空格分隔，留空则不添加任何额外包 ↓'
        required: false
        default: ''
        type: string

env:
  REPO_URL: https://github.com/${{ github.event.inputs.upstream }}
  REPO_BRANCH: master
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: ${{ format('config/{0}.config', github.event.inputs.profile) }}
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Show hardware info
        run: |
          chmod 755 ./scripts/show_hardware_info.sh
          ./scripts/show_hardware_info.sh

      - name: Free Disk Space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: false
          dotnet: false
          haskell: false
          large-packages: false
          docker-images: false
          swap-storage: true

      - name: Initialization environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt-get -y update
          sudo -E apt-get -y install $(curl -fsSL https://raw.githubusercontent.com/xuxin1955/depend_ubuntu2204_openwrt/refs/heads/main/depend_ubuntu2204_openwrt)

      - name: Install Android image tools
        run: |
          wget -O /tmp/mkbootimg.deb https://github.com/xuxin1955/depend_ubuntu2204_openwrt/releases/download/ubuntu_26.04_LTS_mkbootimg/mkbootimg_34.0.5-12build1_all.deb
          sudo dpkg -i /tmp/mkbootimg.deb || sudo apt-get install -f -y
          sudo apt-get update
          sudo apt-get install -y android-sdk-libsparse-utils || true
          command -v mkbootimg
          command -v img2simg || true
          sudo timedatectl set-timezone "$TZ"
          sudo chmod 777 /mnt
          df -hT

      - name: Clone source code
        working-directory: /mnt
        run: |
          git clone "$REPO_URL" -b "$REPO_BRANCH" openwrt
          ln -sf /mnt/openwrt "$GITHUB_WORKSPACE/openwrt"

      - name: Copy files
        run: |
          [ -e files ] && cp -r files openwrt/files || true

      - name: Load custom feeds
        run: |
          [ -e "$FEEDS_CONF" ] && mv "$FEEDS_CONF" openwrt/feeds.conf.default
          chmod +x "$DIY_P1_SH"
          cd openwrt
          "$GITHUB_WORKSPACE/$DIY_P1_SH"

      - name: Update feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a

      - name: Remove conflicting packages
        run: |
          rm -rf openwrt/feeds/smpackage/{base-files,dnsmasq,firewall*,fullconenat,libnftnl,nftables,ppp,opkg,ucl,upx,vsftpd*,miniupnpd-iptables,wireless-regdb} || true

      - name: Install feeds
        run: |
          cd openwrt
          ./scripts/feeds install -a -f

      - name: Load custom configuration
        run: |
          [ -e files ] && mv files openwrt/files || true
          [ -e "$CONFIG_FILE" ] && mv "$CONFIG_FILE" openwrt/.config
          chmod +x "$DIY_P2_SH"
          cd openwrt
          "$GITHUB_WORKSPACE/$DIY_P2_SH"

      - name: Install extra packages
        if: ${{ inputs.extra_packages != '' }}
        run: |
          cd openwrt
          PACKAGES=$(echo '${{ inputs.extra_packages }}' | sed 's/,/ /g')
          for pkg in $PACKAGES; do
            [ -n "$pkg" ] && echo "CONFIG_PACKAGE_$pkg=y" >> .config
          done

      - name: SSH debug
        uses: mxschmitt/action-tmate@master
        if: ${{ inputs.ssh || contains(github.event.action, 'ssh') }}

      - name: Config defconfig
        id: config
        run: |
          cd openwrt
          make defconfig

      - name: Download package
        id: package
        run: |
          cd openwrt
          make download -j4 V=s
          find dl -size -1024c -exec rm -f {} \;

      - name: Show openwrt directory
        run: |
          ls -la openwrt/
          ls -l openwrt/.config

      - name: Upload generated config
        uses: actions/upload-artifact@main
        with:
          name: build-config
          path: |
            ${{ github.workspace }}/openwrt/.config
            ${{ github.workspace }}/openwrt/feeds.conf.default
          include-hidden-files: true

      - name: Compile firmware
        id: compile
        run: |
          cd openwrt
          make -j$(nproc) || make -j1 V=s
          echo "status=success" >> "$GITHUB_OUTPUT"
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
          [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> "$GITHUB_ENV"
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> "$GITHUB_ENV"

      - name: Get kernel version
        run: |
          KERNEL_VER=$(cat /mnt/openwrt/build_dir/target-*/linux-*/linux-*/.kernelversion 2>/dev/null | head -1 || echo "未知")
          echo "KERNEL_VER=$KERNEL_VER" >> "$GITHUB_ENV"

      - name: Organize files
        id: organize
        if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
        run: |
          cd openwrt/bin/targets/*/*
          rm -rf packages
          PROFILE=${{ github.event.inputs.profile }}
          mkdir -p flash_package
          BOOT_IMG=$(ls *boot.img.gz *boot.img 2>/dev/null | head -1)
          SYSTEM_IMG=$(ls *system.img.gz *system.img 2>/dev/null | head -1)
          if [ -z "$BOOT_IMG" ] || [ -z "$SYSTEM_IMG" ]; then
            echo "::error::固件文件未找到"
            ls -la
            exit 1
          fi
          cp "$BOOT_IMG" flash_package/boot.img.gz
          cp "$SYSTEM_IMG" flash_package/system.img.gz
          gzip -dk flash_package/boot.img.gz || true
          gzip -dk flash_package/system.img.gz || true
          rm -f flash_package/boot.img.gz flash_package/system.img.gz
          cp -r "$GITHUB_WORKSPACE/刷机脚本/." flash_package/ || true
          ZIP_NAME="OpenWrt_${PROFILE}${{ env.FILE_DATE }}_刷机包.zip"
          cd flash_package && zip -r "../$ZIP_NAME" . && cd ..
          mkdir -p release_files
          mv "$ZIP_NAME" release_files/
          cp *.manifest release_files/ 2>/dev/null || true
          cp *.buildinfo release_files/ 2>/dev/null || true
          cp sha256sums release_files/ 2>/dev/null || true
          echo "FIRMWARE=$PWD/release_files" >> "$GITHUB_ENV"
          echo "status=success" >> "$GITHUB_OUTPUT"

      - name: Upload firmware directory
        uses: actions/upload-artifact@main
        if: steps.organize.outputs.status == 'success' && !cancelled()
        with:
          name: OpenWrt_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}

      - name: Generate release tag
        id: tag
        if: env.UPLOAD_RELEASE == 'true' && !cancelled()
        run: |
          PROFILE=${{ github.event.inputs.profile }}
          echo "release_tag=$(date +"%Y.%m.%d-%H%M")-${PROFILE}" >> "$GITHUB_OUTPUT"
          touch release.txt
          {
            echo "## ImmortalWrt master分支 高通410系列 · ${PROFILE}"
            echo ""
            echo "| 项目 | 信息 |"
            echo "|------|------|"
            echo "| 编译者 | ${{ github.actor }} |"
            echo "| 机型 | ${PROFILE} |"
            echo "| 内核版本 | ${{ env.KERNEL_VER }} |"
            echo "| 管理地址 | 192.168.1.1 |"
            echo "| 默认用户 | root |"
            echo "| 默认密码 | 空 |"
          } >> release.txt
          echo "status=success" >> "$GITHUB_OUTPUT"

      - name: Upload firmware to release
        if: env.UPLOAD_RELEASE == 'true' && steps.organize.outputs.status == 'success'
        uses: ncipollo/release-action@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag: ${{ steps.tag.outputs.release_tag }}
          bodyFile: release.txt
          artifacts: ${{ env.FIRMWARE }}/**/*
          artifactErrorsFailBuild: false
          allowUpdates: true
          draft: false
          prerelease: false
